# FastNet 高性能网络库源码与调用链说明文档

本文档详细梳理了 `FastNet` 核心模块的架构设计、线程模型，以及 **TCP**、**HTTP**、**WebSocket**、**UDP** 的核心生命周期流程链路（启动、连接、回调、退出），并深入分析了其 **加解密通信（SSL/TLS）** 与 **内存池/缓冲区复用** 的具体设计实现，以帮助开发者看懂并熟练编写基于本库的通信逻辑。

---

## 1. 四大通信协议的具体生命流程链路

### 1.1 TCP 协议生命周期 (基于 `TcpServer` / `TcpClient`)
TCP 是一种面向连接的、可靠的传输层协议。在 `FastNet` 中，它是最底层的 readiness readiness 事件驱动单元。

#### 1. 启动 (Startup)
*   **服务端**：调用 `TcpServer::start()`。
    1.  创建并配置 `SocketWrapper` 监听套接字（`SocketType::TCP`，开启 `SO_REUSEADDR`）。
    2.  调用 `bind()` 绑定指定 IP 和端口，调用 `listen()` 将套接字置于被动监听状态。
    3.  调用 `SocketWrapper::setNonBlocking(true)` 将监听套接字设置为非阻塞。
    4.  调用 `EventPoller::add()` 将监听 Socket 文件描述符（`fd`）注册至多路复用器，关注 `Read | Error | Close` 事件。
*   **客户端**：调用 `TcpClient::connect()`。
    1.  创建非阻塞 `clientFd` 套接字。
    2.  调用 `SocketWrapper::connect()` 发起异步 TCP 握手。由于是非阻塞，若返回 `WouldBlock` / `EINPROGRESS`，客户端会将 `clientFd` 注册至 `EventPoller` 关注 `Write | Error`，等待写就绪以确认连接建立成功。

#### 2. 连接 (Connection)
*   当客户端完成三次握手，服务端监听 `fd` 产生可读事件：
    1.  `EventPoller::poll()` 唤醒，分发执行监听 `fd` 的可读回调。
    2.  回调进入 `TcpServer::Impl::acceptNewConnections()`，在一个循环中调用 `listenSocket_.accept()` 接收新客户端 `fd`（采用 `SOCK_NONBLOCK` 与 `SOCK_CLOEXEC` 选项）。
    3.  调用 `adoptAcceptedSocket()` 为新连接实例化 `ClientSession` 控制块，并分配全局递增的 `ConnectionId`。
    4.  将该客户端套接字 `fd` 注册至 `EventPoller` 并默认关注 `Read | Error | Close` 事件。
    5.  触发 `ServerClientConnectedCallback`，通知应用层新客户端上线。

#### 3. 回调 (Callbacks)
*   当客户端发送数据，`EventPoller` 触发套接字 `Read` 事件：
    1.  `EventPoller::poll()` 唤醒并执行客户端 `fd` 对应的读取 Lambda 回调。
    2.  回调调用 `ClientSession::handleRead()`：
        *   调用 `BufferPool::allocateBuffer()` 快速获取预分配的内存块。
        *   通过 `SocketWrapper::recv()` 读取数据至缓冲区。
        *   将装有读取数据的 `Buffer` 通过 `OwnedDataReceivedCallback` 回调分发给上层应用（如 `HttpServer` 或 `WebSocketServer`）。

#### 4. 退出 (Exit/Teardown)
*   当对端主动关闭连接，或者发生网络异常：
    1.  `handleRead()` 返回值 $0$（对端关闭）或负值（网络错误）。
    2.  调用 `ClientSession::close()`：
        *   调用 `EventPoller::remove(fd)` 从事件轮询器中安全移除。
        *   调用 `SocketWrapper::close()` 彻底释放并关闭底层操作系统套接字句柄。
    3.  触发 `ServerClientDisconnectedCallback` 通知应用层，并在 `TcpServer` 管理映射中擦除该 `ClientSession` 指针。

---

### 1.2 HTTP 协议生命周期 (基于 `HttpServer` / `HttpClient`)
HTTP/1.1 是建立在 TCP 基础之上的“请求-响应”应用层协议。其生命周期本质上是 **TCP 连接的动态建立与解析**。

```text
               +-------------------------------------------------+
               |                  HttpServer::start()            |
               +-----------------------+-------------------------+
                                       | (底层创建并绑定 TCP 监听 Socket)
                                       v
                             TCP 客户端连接握手成功
                                       |
                                       v
                          handleClientConnected()
                                       | (在 map 中分配 ClientState)
                                       v
                                收到网络 TCP 数据
                                       |
                                       v
                            handleDataReceived()
                                       | (数据追加到 ClientState::buffer)
                                       v
                           processBufferedRequests()
                                       |
                        +--------------+--------------+
                        |                             |
                        v                             v
           [数据不够，等待后续 Read]            tryExtractRequest()
                                                      |
                                                      v
                                            [解析出完整 HTTP 头/体]
                                                      |
                                                      v
                                              dispatchRequest()
                                                      |
                                                      v
                                            [路由匹配 RequestHandler]
                                                      |
                                                      v
                                          [业务逻辑 / 发送文件响应]
                                                      |
                                                      v
                                            finalizeResponse()
                                                      |
                                                      v
                                            [若 Keep-Alive == false]
                                                      |
                                                      v
                                        closeClientAfterPendingWrites()
```

#### 1. 启动 (Startup)
1.  调用 `HttpServer::start()`。
2.  在 `HttpServer::Impl` 中注册底层的 `TcpServer` 事件回调，包含：
    *   `setClientConnectedCallback` -> 绑定至 `handleClientConnected`。
    *   `setOwnedDataReceivedCallback` -> 绑定至 `handleDataReceived`。
    *   `setClientDisconnectedCallback` -> 绑定至 `handleClientDisconnected`。
3.  调用 `TcpServer::start()` 启动底层 TCP 监听工作（逻辑如 1.1 节所述）。

#### 2. 连接/请求 (Connection/Request)
1.  当底层 TCP 握手成功，触发 `handleClientConnected()`：
    *   在 `clients_` 映射表中为该 `ConnectionId` 分配一个 `ClientState`，维护解析缓冲区和心跳计时器。
2.  客户端发送 HTTP 报文，触发 `handleDataReceived()`：
    *   将字节流追加至该客户端的 `ClientState::buffer`。
    *   调用 `processBufferedRequests()` 执行流式请求提取。

#### 3. 回调 (Callbacks)
1.  在 `processBufferedRequests` 循环中，调用 `tryExtractRequest()` 进行报文切割。
2.  调用 `HttpParser::parseRequestHead()` 解析 HTTP 头信息（解析 Method、URI、Headers、Query String）。
3.  若存在 Body（根据 `Content-Length` 或 `Transfer-Encoding: chunked` 识别），则继续流式读取，直至数据完整。
4.  一旦请求完全就绪，调用 `dispatchRequest()` 进行路由匹配。
5.  在 `handlers_` 路由表中找到对应的 `RequestHandler`（如 `exactHandler(request, response)`），执行用户的应用层业务回调代码。
6.  业务回调执行完毕后，调用 `finalizeResponse()` 组装返回的 Headers，并通过 `tcpServer_.sendToClient()` 发送回客户端。

#### 4. 退出 (Exit/Teardown)
1.  若请求中包含 `Connection: close`（或者非 Keep-Alive 协议）：
    *   调用 `tcpServer_.closeClientAfterPendingWrites(clientId)` 投递断开指令（写完数据后关闭）。
2.  底层 TCP 断开时，触发 `handleClientDisconnected(clientId)`，将该客户端的 `ClientState` 从 `clients_` map 中安全抹除，释放其拥有的接收缓冲和计时器。

---

### 1.3 WebSocket 协议生命周期 (基于 `WebSocketServer` / `WebSocketClient`)
WebSocket 是一种在单个 TCP 连接上进行全双工通信的协议。它的生命周期具有独特的 **“HTTP 握手升级 (Upgrade)”** 特征。

#### 1. 启动 (Startup)
1.  调用 `WebSocketServer::start()` 引导底层 TCP 监听器。
2.  挂载 TCP 读写和状态回调到 `WebSocketServer::Impl` 的 `handleClientConnected`、`handleDataReceived` 及 `handleClientDisconnected`。

#### 2. 升级/握手 (Upgrade & Handshake)
1.  客户端连接后，初始 `ClientState::Status` 设为 `Handshake`（握手阶段）。
2.  客户端发送符合 RFC 6455 规范的 HTTP Get 请求：
    *   请求头包含 `Upgrade: websocket` 以及 `Sec-WebSocket-Key: <key>`。
3.  `handleHandshake()` 调用 `HttpParser::parseRequest()` 解析头字段。
4.  调用用户的 `handshakeCallback_` 回调函数。如果被允许，则利用 SHA-1 算法和 Base64 对 `Sec-WebSocket-Key` 和魔法字符串 `"258EAFA5-E914-47DA-95CA-C5AB0DC85B11"` 进行签名计算，生成 `Sec-WebSocket-Accept` 头响应。
5.  通过 `TcpServer` 回复 `101 Switching Protocols` 报文。
6.  握手成功，将 `ClientState::Status` 切换为 `Connected`，客户端连接完成升级！

#### 3. 回调 (Callbacks)
1.  连接升级后，客户端发出的后续数据皆以 WebSocket 帧（Frame）包装。
2.  触发 `handleFrames(clientId)`，通过 `WebSocketProtocol::parseFrame()` 解析帧结构（处理 Fin 标记、Opcode 帧操作码、掩码解密 Masking）。
3.  帧解析完成并完成多帧重组后，根据帧类型调用对应应用层业务回调：
    *   **文本帧 (Text)**：触发 `setMessageCallback`。
    *   **二进制帧 (Binary)**：触发 `setBinaryCallback` / `setOwnedBinaryCallback`。
    *   **控制帧 (Ping/Pong)**：自动回复 Pong 帧以维系心跳活度，无需上层业务介入。

#### 4. 退出 (Exit/Teardown)
1.  **主动关闭**：业务代码调用 `disconnectClient(clientId, code, reason)`：
    *   向对端发送 `Close` 帧（Opcode = 0x8，包含关闭码如 1000）。
    *   将连接状态标为 `Closing`，等待写缓冲区数据清空后彻底调用 `tcpServer_.disconnectClient()` 关闭连接。
2.  **被动关闭**：当收到对端发出的 `Close` 帧，自动回复对方一个 `Close` 帧并切断 TCP 连接。
3.  触发 `handleClientDisconnected(clientId)` 彻底清空并销毁连接上下文状态。

---

### 1.4 UDP 协议生命周期 (基于 `UdpSocket`)
UDP 是一种无连接、不可靠的传输层协议。没有连接状态、心跳维护或握手流程。

#### 1. 启动 (Startup)
1.  实例化 `UdpSocket` 并调用 `UdpSocket::bind(localAddress)`。
2.  底层通过 `SocketWrapper::create(SocketType::UDP)` 创建 UDP 套接字。
3.  依次调用底层的 `bind()` 绑定本地监听地址，调用 `setNonBlocking(true)` 设置套接字为非阻塞模式。
4.  设置接收与发送缓冲区（`SO_RCVBUF` / `SO_SNDBUF`）。

#### 2. 接收/发送 (Send & Recv)
*   **发送**：调用 `sendTo(destination, data)`。
    *   由于 UDP 是无连接的，直接调用底层 `SocketWrapper::sendTo()` 将报文打包发送给目标 IP/Port。发送完成后，数据即送往内核，没有发送状态维系。
*   **接收**：调用 `startReceive()`。
    *   将该 Socket 注册至全局 `EventPoller`，监听 `Read | Error` 事件。

#### 3. 回调 (Callbacks)
1.  当网卡收到发送到本端口的 UDP 报文，`EventPoller` 触发 `Read` 事件。
2.  回调进入 `UdpSocket::Impl::drainReceiveQueue()`：
    *   在一个循环中，反复调用 `socket_.recvFrom(receiveScratch_, ...)` 获取数据以及发送方的源地址 `Address(fromIp, fromPort)`。
    *   若返回 `WouldBlock` 则代表当前网卡接收缓冲区已空，退出循环。
    *   若读到有效数据，则直接分发触发 `UdpDataReceivedCallback(senderAddress, packet)`，通知用户在 Poller 线程中处理数据包。

#### 4. 退出 (Exit/Teardown)
1.  调用 `stopReceive()`。
2.  通过 `EventPoller::remove(fd)` 从事件轮询器中注销套接字。
3.  调用 `SocketWrapper::close()` 释放操作系统 UDP 资源。

---

## 2. 安全加解密通信链路 (SSL/TLS)

`FastNet` 的加解密设计基于 OpenSSL 开发，利用非阻塞式的 **内存 BIO (Memory BIO)** 机制实现了与底层网络传输（`EventPoller`）的完全解耦。

```text
    +-------------------------------------------------------------+
    |                         ClientSession                       |
    +-------------------------------------------------------------+
         |                                                   ^
         | [1] 收到加密数据 (Ciphertext)                        | [4] 写入明文数据
         v                                                   |
    +-------------+                                    +-------------+
    | sslReadBio_ |                                    | sslWriteBio_|
    +-------------+                                    +-------------+
         | (内存缓冲区)                                       ^ (内存缓冲区)
         v                                                   |
    +----------------------------------------------------------------+
    |                      sslHandle_ (SSL*)                         |
    +----------------------------------------------------------------+
         | [2] 解密过程 (SSL_read)                              ^ [3] 加密过程 (SSL_write)
         v                                                   |
    +----------------------------------------------------------------+
    |                  plaintext -> HTTP/WS 解析器                    |
    +----------------------------------------------------------------+
```

### 2.1 握手与初始化流程
1.  **创建上下文**：调用 `SSLContext` 装载服务器的 CA 链、证书文件（`.crt`）及私钥（`.key`），并设置双向或单向验证属性。
2.  **创建 SSL 对象**：在 `ClientSession` 初始化时，若检测到 SSL 配置，则调用 `SSL_new(ctx)` 创建 `sslHandle_`，并创建两个方向的内存缓冲区：
    *   `sslReadBio_` (输入解密队列)：`BIO_new(BIO_s_mem())`
    *   `sslWriteBio_` (输出加密队列)：`BIO_new(BIO_s_mem())`
    *   通过 `SSL_set_bio(ssl, readBio, writeBio)` 关联。
3.  **握手泵送**：调用 `driveTlsHandshake()`：
    *   调用 `SSL_do_handshake(sslHandle_)` 触发 OpenSSL 的内部握手状态机。
    *   OpenSSL 会在 `sslWriteBio_` 中产生握手应答数据。
    *   `ClientSession` 检查 `sslWriteBio_` 并将其中的握手密文通过常规 `socket_.send` 发送给客户端。
    *   当收到客户端响应的密文后，通过 `BIO_write` 塞入 `sslReadBio_`，再次驱动 `SSL_do_handshake`，直至握手成功。

### 2.2 数据读取与解密链路 (Read & Decrypt)
1.  `EventPoller` 触发套接字 `Read` 事件 -> 进入 `ClientSession::handleRead()`。
2.  直接调用系统的 `recv`，将网络上接收到的**加密数据 (Ciphertext)** 读入一个临时缓冲区。
3.  调用 `BIO_write(sslReadBio_, buf, len)`，将读到的密文直接送入解密输入管道。
4.  进入一个循环，调用 `SSL_read(sslHandle_, plainBuf, max)`，OpenSSL 将密文自动解密为**明文数据 (Plaintext)** 并写给 `plainBuf`。
5.  将解密出的明文数据传递给上层协议解析器（如 `HttpParser::parseRequestHead`）。

### 2.3 数据加密与写入链路 (Encrypt & Write)
1.  上层应用通过 `sendToClient` 发送响应明文 -> 调用 `ClientSession::send()`。
2.  调用 `SSL_write(sslHandle_, plainData, len)`，OpenSSL 将明文数据转换为**加密数据 (Ciphertext)** 并自动缓存到 `sslWriteBio_` 中。
3.  调用 `BIO_read(sslWriteBio_, cipherBuf, max)` 从输出管道里提取加密后的密文。
4.  调用底层的 `SocketWrapper::send()`，将提取出的密文通过网络真正物理发送给客户端。

---

## 3. 内存复用与数据零拷贝设计

在大规模高并发网络应用中，频繁地在堆上分配和销毁变长的 `vector` 或内存块是导致响应延迟抖动、性能衰退的罪魁祸首。`FastNet` 依靠两个高内聚的自研结构解决该痛点：

### 3.1 跨线程无锁内存复用池 (`MemoryPool`)
`MemoryPool` 用于分配固定大小的物理 Block，并对线程局部缓存做了极致优化：
1.  **ThreadCache 局部化**：为了消除全局互斥锁的竞争，每个线程拥有独立的 `ThreadCache` 链表。调用 `allocate()` 时，优先从线程局部缓存中分配，完全没有任何锁开销。
2.  **无锁跨线程回收**：当线程 B 想要释放线程 A 分配的内存块时，不直接操作线程 A 的局部数据，而是调用 `deallocateFromOtherThread`，通过无锁的 **Treiber Stack**（依靠 `compare_exchange_weak` 原子 CAS 操作）将节点推入 `returnQueue_`。
3.  **自适应 Drain**： owning 线程在下一次执行分配时，会自动调用 `drainReturnQueue()`，将 `returnQueue_` 中的节点批量移回主链表中，达到零锁竞争的高效回收。

### 3.2 缓冲区池 (`BufferPool`) 与智能指针自定义析构
网络读写经常使用动态数组（`std::vector<uint8_t>`）。为了防止其频繁销毁重建，`BufferPool` 使用了 C++ 智能指针的 **Deleter** 重载机制：
1.  **分配**：调用 `BufferPool::allocateBuffer()` 时，从单例回收表中检查可用 `Buffer`，调整尺寸并返回：
    ```cpp
    Buffer* buffer = new Buffer();
    ...
    // 捕获 Pool 指针，并重写智能指针的自定义回收析构动作 (Deleter)
    return std::shared_ptr<Buffer>(buffer, [pool](Buffer* ptr) noexcept {
        ptr->clear();         // 保留已分配好的底层 Capacity 容量，只将 Size 设为 0
        pool->returnBuffer(ptr); // 将空闲 Buffer 归还给缓冲池，避免 delete 析构
    });
    ```
2.  **生命周期生命周期**：数据在解析与路由链路中全程通过 `std::shared_ptr<Buffer>` 传递。当应用层业务使用完毕，所有的 `shared_ptr` 计数归零时，自动触发自定义的 Deleter，将该 `Buffer` 无痕塞回池子中，下一次包接收可直接重复使用其现成的 `capacity()`。

---

## 4. 动手实战：如何编写一个高性能通信逻辑

基于上述链路分析，当您自己编写通信代码时，可以快速遵循以下设计模型：

### 4.1 编写 TCP Echo 服务端代码示例
```cpp
#include "FastNet/TcpServer.h"
#include "FastNet/IoService.h"
#include <iostream>

int main() {
    // 1. 初始化并发内核引擎（配置 4 个工作线程）
    FastNet::IoService ioService(4);
    ioService.start();

    // 2. 实例化 TCP 服务端
    FastNet::TcpServer server(ioService);

    // 3. 挂载状态回调：连接建立
    server.setClientConnectedCallback([](FastNet::ConnectionId clientId, const FastNet::Address& addr) {
        std::cout << "Client connected: " << addr.toString() << " (ID: " << clientId << ")\n";
    });

    // 4. 挂载业务回调：收到数据并原样返回 (Echo)
    server.setOwnedDataReceivedCallback([&server](FastNet::ConnectionId clientId, FastNet::Buffer&& data) {
        // 直接回复客户端 (数据会自动复用 BufferPool，写完成后自动安全回收)
        server.sendToClient(clientId, std::move(data));
    });

    // 5. 绑定端口并启动
    FastNet::Error err = server.start(8080, "0.0.0.0");
    if (err.isFailure()) {
        std::cerr << "Server startup failed: " << err.getMessage() << "\n";
        return -1;
    }

    std::cout << "Echo Server is running on port 8080...\n";
    
    // 主线程模拟阻塞运行
    while (ioService.isRunning()) {
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
    return 0;
}
```

### 4.2 编写 HTTPS 路由处理器示例
```cpp
#include "FastNet/HttpServer.h"
#include <iostream>

int main() {
    FastNet::IoService ioService;
    ioService.start();

    FastNet::HttpServer httpServer(ioService);

    // 注册自定义 HTTP GET 路由 /api/v1/status
    httpServer.get("/api/v1/status", [](const FastNet::HttpRequest& req, FastNet::HttpResponse& resp) {
        resp.statusCode = 200;
        resp.statusMessage = "OK";
        resp.headers["Content-Type"] = "application/json; charset=utf-8";
        resp.body = R"({"status":"active","engine":"FastNet-v2.0"})";
    });

    // 启动开启 SSL 的 HTTPS 服务端
    FastNet::SSLConfig sslConfig(true);
    sslConfig.certificateFile = "./server.crt";
    sslConfig.privateKeyFile = "./server.key";

    if (httpServer.start(443, "0.0.0.0", sslConfig).isFailure()) {
        std::cerr << "Failed to start HTTPS server.\n";
        return -1;
    }

    std::cout << "HTTPS Server running on port 443...\n";
    // ...
}
```
